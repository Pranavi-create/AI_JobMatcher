# Set to a PyPI-available release. If you need features from 2.3.x, upgrade
# your Python runtime to >=3.10 and then update this requirement.
linkedin-api>=2.2.0
# FastMCP requires Python >= 3.10. Make this optional for older environments so
# `pip install -r requirements.txt` won't fail on Python < 3.10.
fastmcp>=0.1.0; python_version >= "3.10"
python-dotenv>=1.0.0
requests>=2.31.0
pydantic>=2.5.3
uvicorn>=0.24.0
